import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF0D631B);
  static const Color primaryLight = Color(0xFF3D8B49);

  // Background
  static const Color background = Color(0xFFF8F9FB);

  // Surface
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF191C1D);
  static const Color textSecondary = Color(0xFF3E4A3E);
  static const Color hint = Color(0xFF707A6C);

  // Border
  static const Color border = Color(0x4CBFCABA);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);

  // Others
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}